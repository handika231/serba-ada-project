
enum TypeImage { File, NetworkSvg, Network, Assets, NetworkFullPath }
enum FontFamily { Poppins, OpenSans, Averta }

enum BuildFlavor { Prod, Stg, Dev }

enum MobilePlatform { Android, IOS }

enum CardProfileView { Information, Action }

enum Gender { Female, Male }

enum Status { Employee, Student }

enum Profession { Employee, Student }

enum FilterSearch { latest, oldest }

enum Companion { ready, not_ready }

enum OTPPage { Register, Forgot, Update_Profil, Login }

enum ButtonStatePlayer { paused, playing, loading }

enum ChooseInterestStatus { Insert, Edit }


enum StatusPage { Parent, Child }

enum GetPermission { Camera, Storage }

enum StatusDownloading { Audio, Image, Video, File, PDFArticle }

enum TypeNotification { CustomerRequest, Content, Webinar, NotFound }

enum TypeMyClass { OnProgres, OnFinish }

enum TypeSessionCourse { start, finish }
